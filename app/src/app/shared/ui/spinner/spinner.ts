import { Component } from '@angular/core';

/** STYLE_GUIDE §6.5 — 14px ring, signals work in progress. */
@Component({
  selector: 'cm-spinner',
  standalone: true,
  templateUrl: './spinner.html',
  styleUrl: './spinner.scss',
})
export class Spinner {}
